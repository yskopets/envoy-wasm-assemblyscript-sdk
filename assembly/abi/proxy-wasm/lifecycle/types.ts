export const enum OnStartResult {
    Error = 0,
    Ok = 1,
}

export const enum OnValidateResult {
    Error = 0,
    Ok = 1,
}

export const enum OnConfigureResult {
    Error = 0,
    Ok = 1,
}

export const enum OnDoneResult {
    NotDone = 0,
    Done = 1,
}
