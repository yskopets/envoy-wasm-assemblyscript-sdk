/**
 * Log level.
 */
export const enum LogLevel {
    /** Trace. */
    Trace = 0,
    /** Debug. */
    Debug = 1,
    /** Info. */
    Info = 2,
    /** Warn. */
    Warn = 3,
    /** Error. */
    Error = 4,
    /** Critical. */
    Critical = 5,
}
