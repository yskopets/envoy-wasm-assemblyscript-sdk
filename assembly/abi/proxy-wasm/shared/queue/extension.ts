
import { ContextId } from "../../context/types";
import { QueueHandle } from "./types";

export declare function proxy_on_queue_ready(
    root_context_id: ContextId,
    queue: QueueHandle,
): void;
